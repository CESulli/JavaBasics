package Class10;

public class E1StringDemo {
    public static void main(String[] args) {
        //we have created an object from the String class and have stored Cat inside of it.
        String obj=new String("Cat");
        //shortcut to create objs from String class
        String obj2="Dog"; //String obj2=new String("Dog")
        int len=obj.length(); //calling the length method/function.action/behavior
        System.out.println(len);
    }
}
